import { allow,json,fail,requireAdmin,adminServices } from '../_server.js';
export default async function handler(req,res){allow(res);if(req.method==='OPTIONS')return json(res,204,{});if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});try{await requireAdmin(req);const {uid}=req.body||{};if(!uid)return json(res,400,{error:'Student UID required.'});const {adminAuth,adminDb}=adminServices();await adminDb.collection('students').doc(uid).delete();await adminAuth.deleteUser(uid).catch(e=>{if(e.code!=='auth/user-not-found')throw e});json(res,200,{ok:true});}catch(e){fail(res,e)}}
